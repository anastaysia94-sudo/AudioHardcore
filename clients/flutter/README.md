# AudioHardcore Flutter Client Scaffold

Future shared client direction for Windows, macOS, and Android/Fire OS. The current repository also contains a native Android Studio foundation. Flutter compilation is not claimed until an SDK-backed CI/build environment is provisioned.
